{
  "name": "camion-app-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "live-server public --port=3000",
    "build": "mkdir -p dist && cp -r public/* dist/",
    "start": "live-server dist --port=3000",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
