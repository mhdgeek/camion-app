{
  "name": "camion-app-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "live-server public --port=3000",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp vercel.json dist/",
    "start": "live-server dist --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
